# Solver control (later)

Leave in the document and session:

- Solver registry (name, command, working dir, input pattern)
- Job: start, capture stdout, cancel, mark failed/succeeded
- Link from case export file to job

No in-process PDE solver is required. Remote submit is optional after local jobs work.

Do not add a Solve menu until these commands exist.
